[phases.setup]
nixPkgs = ['nodejs_20', 'openssl']
nixLibs = ['openssl']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = [
  'cd backend && npx prisma generate',
  'npm run build'
]

[start]
cmd = 'npm run start'
